{
  "extends": ["../../../.eslintrc.json"],
  "ignorePatterns": ["!**/*"],
  "overrides": [
    {
      "files": ["*.json"],
      "parser": "jsonc-eslint-parser",
      "rules": {
        "@nx/dependency-checks": [
          "error",
          {
            "ignoredDependencies": ["@nx/devkit", "vitest", "@nx/vite", "@swc/helpers"] // these libs will be omitted from checks
          }
        ]
      }
    }
  ]
}
